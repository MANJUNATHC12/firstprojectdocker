server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 🔥 ADD THIS BLOCK
    location /Register {
        proxy_pass http://backend:8081/Register;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}